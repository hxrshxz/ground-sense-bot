# Gemini API Configuration
# Required for AI features when deployed online (without local Ollama LLM)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Specify the Gemini model to use (defaults to gemini-2.5-flash)
VITE_GEMINI_MODEL=gemini-flash-latest
